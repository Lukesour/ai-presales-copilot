# AI Presales Copilot 任务进展

本页只索引当前需要跨模块、契约或治理协调的工作。单个函数或单个测试的日常修改不在此排期。

## 状态口径

- `Active`：正在实现或验证。
- `Waiting`：等待明确输入、设备、依赖或授权。
- `Blocked`：存在已确认且尚未解除的阻塞条件。
- 完成任务移动到 `docs/tasks/archive/<year>/`，不在活跃表保留历史行。

## 活跃任务

当前没有需要跨模块协调的活跃任务。新任务应先创建 `docs/tasks/active/<TASK-ID>.md`，再登记到本页。

## 维护规则

- Task ID 全局唯一；标题变化不改变 ID。
- 叶页必须记录目标、非目标、责任人、范围、兼容策略、验收、验证和阻塞。
- 根 memory 只保留仍影响下一步的决策、风险和工作，不复制任务详情。
- 完成页移动到年度归档目录；Git 保留历史，索引保持当前。
- 已完成的治理基线、OpenAPI 契约和 clean-break 任务归档于
  `docs/tasks/archive/2026/`，不回写到本页的活跃索引。
- 不在本页记录生产凭据、设备排期、外部授权或未脱敏运行输出。
