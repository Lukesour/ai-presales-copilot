# Security policy

这是一个本地运行、面向作品集的售前工作台。默认 Compose token 只适合单机演示，不能直接暴露到公网，也不能替代 OIDC/JWT、密钥管理、TLS、网络策略或生产审计平台。

报告安全问题时，请不要提交真实客户资料、密钥、模型权重或包含 PII 的日志。优先使用仓库 issue 的私密渠道；如果当前仓库没有私密渠道，请先联系维护者，再提供最小化复现、影响范围、版本/commit 和修复建议。

已覆盖的边界包括：

- 运行前的租户、项目、用户和角色检查，以及检索前 ACL 过滤；
- 本地/授权来源白名单、HTTPS、robots 检查、SSRF 私网地址阻断和大小限制；
- PII、密钥、提示词注入和未经证据支持的承诺扫描；
- JSON Schema 二次校验、未知证据 ID 丢弃、有限结构化重试和 `model_unavailable` 拒答；
- 审核乐观锁、版本号、幂等键和脱敏事件记录。

这些检查不能证明系统绝对安全。共享部署前必须完成 OIDC/JWT 接入、secret rotation、TLS、容器/依赖扫描、数据库备份恢复、审计访问控制和针对真实资料的红队复核。
